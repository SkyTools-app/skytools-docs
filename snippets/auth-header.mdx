All API requests require an `X-API-Key` header. Get your key from the [Developer Dashboard](https://developer.skytools.app/keys).

```bash
curl -H "X-API-Key: sk_your_key_here" \
  "https://api.skytools.app/v1/..."
```

See [Authentication](/authentication) for details on key formats and tier limits.
